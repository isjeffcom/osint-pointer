# MiniMax API（必填）：用于黑天鹅事件分析与概率推断
# 在 https://platform.minimax.io 创建 API Key（账户管理 -> 接口密钥）
MINIMAX_API_KEY=your_minimax_api_key

# 可选：API 基础 URL，不填则默认 https://api.minimax.io（官方 Text 接口）
# LLM_BASE_URL=https://api.minimax.io

# 可选：模型名称，不填则默认 MiniMax-M2.1-highspeed（更快、长 JSON 更稳）
# 其它选项：MiniMax-M2.5、MiniMax-M2.5-highspeed、MiniMax-M2（见 platform.minimax.io 文档）
# LLM_MODEL=MiniMax-M2.1-highspeed

# 事记用 Cloudflare KV：在 wrangler.toml 里绑定 OSINT_POINTER_KV 即可，本地与部署都用该绑定，无需 API Token。
